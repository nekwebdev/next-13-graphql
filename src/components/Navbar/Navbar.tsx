// Components
import { NavDesktop, NavMobile } from '@components/Navbar'

const Navbar = () => {
  return (
    <nav>
      {/* Navigation bar */}
      <NavDesktop />
      <NavMobile />
    </nav>
  )
}

export default Navbar
